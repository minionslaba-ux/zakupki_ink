# Зависимости nev-calculator.
#
# Ядро (модели, каталог из JSON, ценообразование, расчёт, курсы ЦБ)
# работает на чистой стандартной библиотеке Python 3.10+ — зависимостей нет.
#
# Дополнительно:
openpyxl>=3.1      # чтение «живой» книги Калькулятор_NEV*.xlsx (Catalog.from_xlsx)

# Для разработки и тестов:
#   pip install pytest
pytest>=7.0
